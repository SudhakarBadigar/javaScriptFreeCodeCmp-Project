/** 
    Data types undefined(that hasn't been defined),boolean,null,
    number,string,symbol(immutable primitive value) and Object 
*/

var myName="Sudhakar";// var is used in the whole program.

myName=8;//this will change the myName Sudhakar to 8.               
console.log(myName);

let ourName ="prostackcamp";//let is used only in the scope where you declared that

const pi = 3.14//this is constant value for pi.

//pi=2.233; you cannot change value of constant .

console.log(ourName);
console.log(pi);
/**
 there are three ways you can declare a variable
 1.var
 2.let
 3.const
  */

